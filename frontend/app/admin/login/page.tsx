'use client';
import {useState} from 'react';
import {useRouter} from 'next/navigation';
const api=process.env.NEXT_PUBLIC_API_URL||'http://localhost:8000/api/v1';
export default function AdminLogin(){const router=useRouter();const [email,setEmail]=useState('admin@medicii.example.com'),[password,setPassword]=useState('ChangeMe123!'),[notice,setNotice]=useState('');async function submit(e:React.FormEvent){e.preventDefault();const r=await fetch(api+'/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,password})});const x=await r.json();if(!r.ok){setNotice(typeof x.detail==='string'?x.detail:'Unable to sign in.');return;}if(x.role!=='ADMIN'){setNotice('This account does not have Medicii operations access.');return;}sessionStorage.setItem('medicii_access_token',x.access_token);sessionStorage.setItem('medicii_role',x.role);router.push('/admin');}return <main className="admin-login"><nav><b><i>✦</i> medicii <small>Operations</small></b></nav><section className="panel"><p className="eyebrow">RESTRICTED ACCESS</p><h1>Operations sign in</h1><p>For authorized Medicii team members only.</p><form onSubmit={submit}><input type="email" value={email} onChange={e=>setEmail(e.target.value)}/><input type="password" value={password} onChange={e=>setPassword(e.target.value)}/><button>Sign in to operations →</button></form>{notice&&<p className="notice">{notice}</p>}</section></main>}
