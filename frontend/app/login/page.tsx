'use client'; import {useState} from 'react'; import Link from 'next/link'; import {useRouter} from 'next/navigation'; const api=process.env.NEXT_PUBLIC_API_URL||'http://localhost:8000/api/v1';
export default function Login(){const router=useRouter(),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[notice,setNotice]=useState('');async function submit(e:React.FormEvent){e.preventDefault();const r=await fetch(api+'/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,password})});const x=await r.json();if(!r.ok){setNotice(typeof x.detail==='string'?x.detail:'Unable to sign in.');return;}sessionStorage.setItem('medicii_access_token',x.access_token);sessionStorage.setItem('medicii_role',x.role);router.push(x.role==='ADMIN'?'/admin':'/dashboard');}return <main className="auth"><Link href="/">← Back to Medicii</Link><section className="panel"><p className="eyebrow">WELCOME BACK</p><h1>Sign in</h1><form onSubmit={submit}><input required type="email" placeholder="Email address" value={email} onChange={e=>setEmail(e.target.value)}/><input required type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)}/><button>Sign in →</button></form><p><Link href="/forgot-password">Forgot password?</Link></p><small>New to Medicii? <Link href="/register">Create an account</Link></small>{notice&&<p className="notice">{notice}</p>}</section></main>}
