import './style.css';
export const metadata={title:'Medicii | Medicine, thoughtfully sourced',description:'Secure medicine quote and ordering'};
export default function Root({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
